# Training Ground Scene Setup Plan

This is only a manual future plan. No real `.unity` scene file was created in Step 13.

## Future Manual Scene

Create a Unity scene named:

- `TrainingGroundScene`

Save it later under:

- `Assets/CrazeVerse/Scenes/`

## Future Empty GameObjects

Create these GameObjects manually later in Unity Editor:

- `GameManager`
- `UIManager`
- `TrainingGroundSetup`
- `PlayerPreview`
- `CameraRig`
- `SpawnPoints`

## Future Components To Add

Add these components manually later:

- `TrainingGroundSetupController`
- `TrainingStationRegistry`
- `TrainingPlayerSpawnPlanner`
- `TrainingLoadoutPlanner`
- `LocalPlayerController`
- `PlayerInputReader`
- `ThirdPersonCameraRig`
- `WeaponController`
- `HealthController`
- `InventoryController`

## Future Inspector Wiring

Wire references manually in the Inspector only after the scripts compile with zero Console errors.

## Important

This document is a plan only. Step 13 does not create a scene, prefab, terrain, real player, real camera rig, real movement, real weapon use, or gameplay.

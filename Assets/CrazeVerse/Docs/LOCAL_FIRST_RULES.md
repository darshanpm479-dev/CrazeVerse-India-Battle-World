# Local First Rules

The first Unity prototype must work offline with guest access, local profile, local save, local coins, XP, rank, mock rooms, mock leaderboard, mock shop, bot teammates, and local mission data. Firebase, backend, payment, online services, and external API setup are later tasks only.
